import { Layout } from '../components/layout';

const Home = () => {
  return (
    <Layout>
      <h1>React D3.js Examples</h1>
    </Layout>
  );
};

// eslint-disable-next-line import/no-default-export
export default Home;
